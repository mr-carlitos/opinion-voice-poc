{
  "name": "opinion-voice-poc",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "uv run --frozen uvicorn app.main:app --host 127.0.0.1 --port 8000 --no-access-log",
    "lint": "uv run --frozen ruff check . && uv run --frozen ruff format --check . && node --check app/static/app.js && node --check playwright.config.js && node --check tests/e2e/startup.spec.js",
    "test:python": "uv run --frozen pytest --junitxml=artifacts/pytest.xml",
    "test:e2e": "playwright test",
    "test": "npm run lint && npm run test:python && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "1.61.0"
  }
}